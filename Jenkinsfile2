pipeline {
   agent {
       label 'slave1'
   }
   stages {
       stage('Execute shell') {
            steps {
                  sh "echo 'Hello,This is test for webhook'"
            }      
       }
   }    
}
